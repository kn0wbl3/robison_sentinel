services:
  robison_website:
    build:
      context: .
      dockerfile: ./tests/integration/server/server.Dockerfile
    init: true
    ports:
      - 5000:5000
    healthcheck:
      test: 'nc -vz localhost 8080'
      interval: 3s
    command:
      - /workarea/server.sh
  
  # client:
  #   build:
  #     context: .
  #     dockerfile: ./tests/integration/client/client.Dockerfile
  #   init: true
  #   depends_on:
  #     robison_website:
  #       condition: service_healthy
  #   command:
  #     - /workarea/client.sh